services:
  - type: web
    name: college-quisqueya-backend
    env: python
    plan: free

    buildCommand: |
      cd backend
      pip install -r requirements.txt
      python manage.py collectstatic --noinput

    startCommand: |
      cd backend
      gunicorn backend.wsgi:application --log-file -

    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: backend.backend.settings   # ✅ utiliser ton settings.py principal
      - key: PYTHON_VERSION
        value: 3.13.4
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: DEBUG
        value: false
      - key: RENDER_EXTERNAL_HOSTNAME
        fromService:
          type: web
          name: college-quisqueya-backend
          property: host
